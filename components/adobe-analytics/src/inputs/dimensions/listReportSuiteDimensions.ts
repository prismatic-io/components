import {
  connectionInput,
  globalCompanyIdInput,
  reportSuiteIdInput,
} from "../common";

export const listReportSuiteDimensionsInputs = {
  connection: connectionInput,
  globalCompanyId: globalCompanyIdInput,
  reportSuiteId: reportSuiteIdInput,
};
