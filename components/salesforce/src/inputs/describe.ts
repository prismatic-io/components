import { connectionInput, recordType, version } from "./common";

export const describeObjectInputs = {
  version,
  recordType,
  connection: connectionInput,
};

export const describePermissionsInputs = {
  version,
  recordType,
  connection: connectionInput,
};
