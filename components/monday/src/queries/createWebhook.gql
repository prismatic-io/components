mutation CreateWebhook(
  $board_id: ID!
  $url: String!
  $event: WebhookEventType!
  $config: JSON
) {
  create_webhook(
    board_id: $board_id
    url: $url
    event: $event
    config: $config
  ) {
    id
    board_id
    event
    config
  }
}
