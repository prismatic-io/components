import { connection } from "./common";
import { changeType, expirationDateTime, resource } from "./subscriptions";
export const resourceTriggerInputs = {
  connection,
  expirationDateTime,
  changeType,
  resource,
};
