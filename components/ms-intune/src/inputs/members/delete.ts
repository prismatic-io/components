import { connection } from "../general";
import { groupId } from "../mobileApps/general";
import { memberId } from "./general";

export const deleteMemberInputs = {
  connection,
  groupId,
  memberId,
};
