import { createBookingByProvider } from "./createBookingByProvider";
import { getBookingByProvider } from "./getBookingByProvider";
import { getBookingByTenant } from "./getBookingByTenant";
import { listBookingByProvider } from "./listBookingByProvider";
import { listBookingByTenant } from "./listBookingByTenant";
import { updateBooking } from "./updateBooking";
export default {
  createBookingByProvider,
  getBookingByProvider,
  getBookingByTenant,
  listBookingByProvider,
  listBookingByTenant,
  updateBooking,
};
