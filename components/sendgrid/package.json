{
  "name": "@prismatic-io/sendgrid",
  "private": true,
  "scripts": {
    "format": "biome format --write .",
    "lint": "biome check --write .",
    "build": "npm run format && npm run lint && tsdown",
    "typecheck": "tsc --noEmit",
    "publish": "npm run build && prism components:publish",
    "test": "vitest run"
  },
  "dependencies": {
    "@prismatic-io/spectral": "10.30.3",
    "@sendgrid/eventwebhook": "^8.0.0",
    "@sendgrid/mail": "7.7.0"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.1",
    "@types/node": "^22",
    "tsdown": "0.22.3",
    "typescript": "6.0.3",
    "vitest": "4.1.9"
  }
}
