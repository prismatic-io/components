{
  "name": "@prismatic-io/intercom",
  "private": true,
  "license": "Apache-2.0",
  "scripts": {
    "lint": "biome check . --write",
    "format": "biome format . --write",
    "build": "npm run format && npm run lint && webpack",
    "publish": "npm run build && prism components:publish",
    "test": "echo 'no tests'"
  },
  "dependencies": {
    "@prismatic-io/spectral": "10.17.1"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.1.4",
    "@types/jest": "^30.0.0",
    "@types/node": "^18.19.121",
    "copy-webpack-plugin": "^13.0.0",
    "jest": "^30.0.5",
    "ts-jest": "^29.4.1",
    "ts-loader": "^9.5.2",
    "typescript": "^5.9.2",
    "webpack": "^5.101.0",
    "webpack-cli": "^6.0.1"
  }
}
