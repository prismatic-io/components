{
  "name": "@prismatic-io/arena-plm-v2",
  "private": true,
  "license": "Apache-2.0",
  "scripts": {
    "lint": "biome lint --write .",
    "format": "biome format --write .",
    "build": "npm run format && npm run lint && webpack",
    "publish": "npm run build && prism components:publish",
    "test": "jest --maxWorkers=4 --testPathIgnorePatterns=integration",
    "test:integration": "jest --maxWorkers=4 --testPathPatterns=integration"
  },
  "dependencies": {
    "@prismatic-io/spectral": "10.21.0",
    "form-data": "^4.0.0"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.1",
    "@types/jest": "30.0.0",
    "@types/node": "22.10.7",
    "copy-webpack-plugin": "14.0.0",
    "jest": "30.4.2",
    "nock": "14.0.17",
    "ts-jest": "29.4.11",
    "ts-loader": "9.6.2",
    "typescript": "5.9.3",
    "webpack": "5.108.2",
    "webpack-cli": "7.1.0"
  }
}
