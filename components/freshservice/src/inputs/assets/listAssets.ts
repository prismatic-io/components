import {
  additionalQueryParams,
  connection,
  fetchAll,
  page,
  perPage,
} from "../common";

export const listAssetsInputs = {
  connection,
  fetchAll,
  perPage,
  page,
  additionalQueryParams,
};
