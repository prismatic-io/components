import {
  additionalQueryParams,
  connection,
  fetchAll,
  page,
  perPage,
} from "../common";

export const listAgentsInputs = {
  connection,
  fetchAll,
  perPage,
  page,
  additionalQueryParams,
};
