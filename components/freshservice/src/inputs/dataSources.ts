import { connection } from "./common";
import { filter } from "./tickets";
export const selectAgentInputs = {
  connection,
};
export const selectAssetInputs = {
  connection,
};
export const selectItamAssetInputs = {
  connection,
};
export const selectItamDeviceInputs = {
  connection,
};
export const selectProblemInputs = {
  connection,
};
export const selectRequesterInputs = {
  connection,
};
export const selectSoftwareInputs = {
  connection,
};
export const selectTicketInputs = {
  connection,
  filter,
};
export const selectWorkspaceInputs = {
  connection,
};
