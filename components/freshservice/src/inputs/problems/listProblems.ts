import {
  additionalQueryParams,
  connection,
  fetchAll,
  page,
  perPage,
} from "../common";

export const listProblemsInputs = {
  connection,
  fetchAll,
  perPage,
  page,
  additionalQueryParams,
};
