import {
  additionalQueryParams,
  connection,
  fetchAll,
  page,
  perPage,
} from "../common";

export const listRequestersInputs = {
  connection,
  fetchAll,
  perPage,
  page,
  additionalQueryParams,
};
