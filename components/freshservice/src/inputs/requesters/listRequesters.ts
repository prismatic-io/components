import {
  additionalQueryParams,
  connection,
  fetchAll,
  pagination,
} from "../common";
export const listRequestersInputs = {
  connection,
  fetchAll,
  pagination,
  additionalQueryParams,
};
