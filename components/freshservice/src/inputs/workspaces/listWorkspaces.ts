import {
  additionalQueryParams,
  connection,
  fetchAll,
  pagination,
} from "../common";
export const listWorkspacesInputs = {
  connection,
  fetchAll,
  pagination,
  additionalQueryParams,
};
