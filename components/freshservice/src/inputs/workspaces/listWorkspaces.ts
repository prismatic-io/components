import {
  additionalQueryParams,
  connection,
  fetchAll,
  page,
  perPage,
} from "../common";

export const listWorkspacesInputs = {
  connection,
  fetchAll,
  perPage,
  page,
  additionalQueryParams,
};
