import { connection } from "../common";
import {
  displayId,
  email,
  quantity,
  requestedFor,
  serviceRequestAdditionalFields,
} from "./common";

export const createServiceRequestInputs = {
  connection,
  displayId,
  quantity,
  email,
  requestedFor,
  serviceRequestAdditionalFields,
};
