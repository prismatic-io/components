import { webhook } from "./webhook";
import { managedWebhook } from "./managedWebhook";
import { newOrUpdatedFile } from "./newOrUpdatedFile";
import { newFolder } from "./newFolder";
import { newFileComments } from "./newFileComments";

export default {
  webhook,
  managedWebhook,
  newOrUpdatedFile,
  newFolder,
  newFileComments,
};
