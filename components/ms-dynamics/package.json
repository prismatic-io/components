{
  "name": "@prismatic-io/ms-dynamics",
  "private": true,
  "scripts": {
    "build": "npm run format && npm run lint && tsdown",
    "typecheck": "tsc --noEmit",
    "lint": "biome check --write .",
    "format": "biome format --write .",
    "publish": "npm run build && prism components:publish",
    "test": "vitest run"
  },
  "dependencies": {
    "@prismatic-io/spectral": "10.28.0",
    "dynamics-web-api": "2.5.0"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.1",
    "@types/node": "^26",
    "nock": "^14.0.0",
    "tsdown": "0.22.3",
    "typescript": "6.0.3",
    "vitest": "4.1.9"
  }
}
