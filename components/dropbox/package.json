{
  "name": "@prismatic-io/dropbox",
  "private": true,
  "scripts": {
    "build": "npm run format && npm run lint && tsdown",
    "typecheck": "tsc --noEmit",
    "lint": "biome check --write .",
    "format": "biome format --write .",
    "publish": "npm run build && prism components:publish",
    "test": "vitest run"
  },
  "dependencies": {
    "@prismatic-io/spectral": "10.30.1",
    "cross-fetch": "3.1.5",
    "dropbox": "10.34.0",
    "lodash.isequal": "4.5.0",
    "mime-types": "2.1.27"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.1",
    "@types/mime-types": "2.1.0",
    "@types/node": "^26",
    "tsdown": "0.22.3",
    "typescript": "6.0.3",
    "vitest": "4.1.9"
  }
}
