import { connectionInput } from "../common";
import {
  fulfillmentId,
  orderId,
  pageFulfillments,
  pageSizeFulfillments,
} from "./common";

export const listFulfillmentsInputs = {
  connectionInput,
  fulfillmentId,
  orderId,
  pageFulfillments,
  pageSizeFulfillments,
};
