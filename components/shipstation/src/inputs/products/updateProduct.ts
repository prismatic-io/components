import { connectionInput } from "../common";
import { productDataInput, productIdInput } from "./common";

export const updateProductInputs = {
  connectionInput,
  productId: productIdInput,
  productData: productDataInput,
};
