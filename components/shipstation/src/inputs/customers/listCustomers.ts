import {
  connectionInput,
  marketplaceId,
  pagination,
  sorting,
  tagId,
} from "../common";
import { countryCode, stateCode } from "./common";
export const listCustomersInputs = {
  connectionInput,
  stateCode,
  countryCode,
  marketplaceId,
  tagId,
  sorting,
  pagination,
};
