import { connectionInput } from "../common";
import { storeIdInput, storeUpdateDataInput } from "./common";

export const updateStoreInputs = {
  connectionInput,
  storeId: storeIdInput,
  storeUpdateData: storeUpdateDataInput,
};
