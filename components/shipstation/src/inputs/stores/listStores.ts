import { connectionInput, showInactive } from "../common";
import { marketplaceIdInput } from "./common";

export const listStoresInputs = {
  connectionInput,
  showInactive,
  marketplaceId: marketplaceIdInput,
};
