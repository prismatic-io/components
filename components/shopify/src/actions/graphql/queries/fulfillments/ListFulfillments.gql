#import "../../fragments/FulfillmentDetails.gql"
query ListFulfillments($orderId: ID!, $first: Int!) {
  order(id: $orderId) {
    fulfillments(first: $first) {
      ...FulfillmentDetails
    }
  }
}
