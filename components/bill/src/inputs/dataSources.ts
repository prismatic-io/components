import { connection } from "./shared";
export const selectBillInputs = { connection };
export const selectCustomerInputs = { connection };
export const selectVendorInputs = { connection };
export const selectInvoiceInputs = { connection };
export const selectCustomerBankAccountInputs = { connection };
export const selectVendorBankAccountInputs = { connection };
