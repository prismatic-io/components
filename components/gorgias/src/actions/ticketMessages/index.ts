import { createTicketMessage } from "./createTicketMessage";
import { getTicketMessage } from "./getTicketMessage";
import { listMessages } from "./listMessages";

export default {
  createTicketMessage,
  getTicketMessage,
  listMessages,
};
