{
  "name": "@prismatic-io/calendly",
  "private": true,
  "scripts": {
    "build": "npm run format && npm run lint && tsdown",
    "typecheck": "tsc --noEmit",
    "lint": "biome lint --write .",
    "format": "biome format --write .",
    "publish": "npm run build && prism components:publish",
    "test": "vitest run"
  },
  "dependencies": {
    "@prismatic-io/spectral": "10.30.1"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.1",
    "@types/node": "^26.4.1",
    "tsdown": "0.22.3",
    "nock": "14.0.5",
    "typescript": "6.0.3",
    "vitest": "4.1.9"
  }
}
