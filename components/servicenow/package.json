{
  "name": "@prismatic-io/servicenow",
  "private": true,
  "scripts": {
    "build": "npm run format && npm run lint && tsdown",
    "typecheck": "tsc --noEmit",
    "lint": "biome check --write .",
    "format": "biome format --write .",
    "publish": "npm run build && prism components:publish",
    "test": "vitest run"
  },
  "dependencies": {
    "@prismatic-io/spectral": "10.30.1",
    "url-join": "4.0.1"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.1",
    "@types/node": "^26",
    "@types/url-join": "4.0.0",
    "nock": "^14.0.17",
    "tsdown": "0.22.3",
    "typescript": "6.0.3",
    "vitest": "4.1.9"
  }
}
