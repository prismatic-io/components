{
  "name": "@prismatic-io/square",
  "private": true,
  "license": "Apache-2.0",
  "version": "0.0.1",
  "description": "Square payment processing component for Prismatic integration platform",
  "scripts": {
    "publish": "npm run build && prism components:publish",
    "build": "npm run format && npm run lint && webpack",
    "test": "echo 'no tests'",
    "lint": "biome check --write .",
    "format": "biome format --write ."
  },
  "dependencies": {
    "@prismatic-io/spectral": "10.17.1"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.4",
    "@types/jest": "^30.0.0",
    "@types/node": "^18.19.130",
    "copy-webpack-plugin": "13.0.1",
    "jest": "^30.2.0",
    "ts-jest": "^29.4.5",
    "ts-loader": "^9.5.4",
    "typescript": "^5.9.3",
    "webpack": "^5.102.1",
    "webpack-cli": "^6.0.1"
  }
}
