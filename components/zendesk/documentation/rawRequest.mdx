The **Raw Request** action allows interaction with API endpoints that are not covered by other actions.
For example, to [list groups](https://developer.zendesk.com/api-reference/ticketing/groups/groups/#list-groups), which has an endpoint of `/api/v2/groups`, enter `/groups` as the endpoint and select the GET verb.
